import { DayNameFormatPipe } from './day-name-format.pipe';

describe('DayNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DayNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
